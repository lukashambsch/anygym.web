const login = (state = {
  isAuthenticating: false,
  authenticated: false,
  username: '',
  password: ''
}, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default login
